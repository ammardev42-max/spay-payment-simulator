  spring:
    application:
      name: SPay

    datasource:
      url: ${DB_URL:jdbc:postgresql://localhost:5432/spay_db}
      username: postgres
      password: Root@123

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
